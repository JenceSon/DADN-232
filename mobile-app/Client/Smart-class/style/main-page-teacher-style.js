import { StyleSheet, Text, View } from 'react-native';
export const mainTeacherStyles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  });